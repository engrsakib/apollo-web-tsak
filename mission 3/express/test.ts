const a = 5;
const b = 10;
const c = a + b;
console.log(c);

// This code adds two numbers and logs the result to the console.

function addNumbers(x:number, y:number): number {
    return x + y;
}
console.log(addNumbers(a, b)); // Outputs: 15